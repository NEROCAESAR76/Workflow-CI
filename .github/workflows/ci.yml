name: CI/CD Pipeline MLflow Docker
on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12.7'

      - name: Install Dependencies
        run: |
          pip install pandas scikit-learn mlflow==2.19.0

      - name: Run MLflow Project
        run: |
          mlflow run . --env-manager local

      - name: Upload Artifacts ke GitHub (Syarat Skilled)
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-artifacts
          path: mlruns/

      - name: Login ke Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build and Push Docker Image (Syarat Advance)
        run: |
          # Ambil ID folder run terbaru dari dalam mlruns/0
          RUN_ID=$(ls -t mlruns/0/ | grep -v meta.yaml | head -1)
          echo "Dapat Run ID: $RUN_ID"
          
          # Build Docker (Tambahkan --env-manager conda di ujungnya)
          mlflow models build-docker -m "runs:/$RUN_ID/random_forest_model" -n "${{ secrets.DOCKER_USERNAME }}/msml-model:latest" --env-manager conda
          
          # Push ke Docker Hub
          docker push "${{ secrets.DOCKER_USERNAME }}/msml-model:latest"